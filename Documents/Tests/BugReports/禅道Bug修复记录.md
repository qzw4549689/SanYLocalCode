# 禅道 Bug 修复记录

> **用途**：登记所有 Bug 及其修复全过程，与禅道 Bug 编号一一关联。
> **规则来源**：根目录 `AGENTS.md` 第 5 节（2026-07-24 起强制执行）。

## 维护规则

1. **先登记后动手**：凡修复 Bug，开工前必须先在本表登记（禅道编号、标题、模块、日期、状态）；AI 修 Bug 前必须先读本表确认已登记。
2. **全过程更新**：根因、改动文件、部署/验证状态记录在同一行，随修复进展持续更新。
3. **状态流转**：`🔧 修复中` → `✅ 已修复待验证`（DEV 验证通过）→ `📋 已关闭`（用户验证 / UAT 发布）。
4. **无禅道编号的 Bug**（口头反馈、自查发现）同样登记，编号列填「无」并备注来源。
5. **commit message 关联禅道编号**，如 `fix(tradestpayterm): 事业部带出及审批共享修复 (#1151)`。

---

## Bug 列表

| 禅道编号 | 标题 | 模块 | 登记日期 | 状态 | 根因 | 改动文件 | 部署/验证记录 | 备注 |
|---------|------|------|---------|------|------|---------|--------------|------|
| 无 | 【成交条件基线库】列表【批量申请/审批/拒绝】按钮未勾选时显示、勾选记录后反而消失（UAT 复现） | 成交条件样板库（`mcs_trade_stpayterm`） | 2026-07-27 | ✅ 已修复待验证 | 微软 KB 4481268「设计使然」：UCI 网格勾选后只显示带选中计数规则的按钮。DEV1 已修复（关联第一方规则 `Mscrm.SelectionCountAtLeastOne` + visibilitytype=2），但 `appaction_appactionrule_classicrules` N:N 关联**不随 Solution 导入**，UAT 按钮随包到达后无规则关联，退回默认反显行为 | `Code/Tools/DeployTool/AppActionDeployer.cs`（`AttachFirstPartySelectionCountRule` 第一方规则 ID 改为按 uniquename 反查，规则 ID 各环境不同：DEV1=`f39219dc`/UAT=`3d0c1736`）<br>`Code/Tools/MetadataTool/Program.cs`（新增只读命令 `list-appaction-rules` 逐环境核对规则关联） | ✅ 2026-07-27 上午 DEV1 修复并 Playwright 实测通过（未勾选隐藏、勾选显示）<br>✅ 2026-07-27 下午 UAT 实测定论：按钮记录与 DEV1 一致（visibilitytype=2、JS 参数 23+12），仅缺规则关联；非缓存问题<br>✅ UAT 已对 apply/approve/reject 执行 `attach-selection-rule`，`list-appaction-rules` 回读与 DEV1 一致<br>✅ 2026-07-27 用户 UAT 验证通过（清除站点数据后：未勾选隐藏、勾选显示）<br>✅ **2026-07-27 深夜架构升级（生产零手动约束）**：3 个按钮改 RibbonDiffXml（`Code/Customizations/Ribbon/mcs_trade_stpayterm.ribbon.xml`，内联 SelectionCountRule 随实体包走）；DEV1 已删旧 appaction 并从实体包/主清单移除；DEV1 Playwright 全量验证通过（显隐/批量申请 0→1/批量拒绝 1→0/列表自动刷新，测试数据已还原）；重新导出包回读确认 Ribbon 随实体导出、appactions 仅剩 clone<br>✅ 2026-07-28 用户已 n8n 发布 `entity_20260727_peter` 到 UAT，新 Ribbon 按钮生效（溢出菜单）<br>⚠️ **2026-07-28 发现「托管 Upgrade 自动删旧按钮」假设不成立**：UAT 实体包均为**非托管**导入，非托管导入只做叠加更新、平台永不删除组件，UAT 3 个旧 appaction（`mcs_trade_stpayterm_apply/approve/reject`）残留导致主命令栏新旧按钮重复并存（`list-app-actions` 只读确认）；用户在 UAT Command Designer 手动**隐藏**旧按钮（未删除，可逆）；`list-app-actions cr0c0` 回读确认未生成 `cr0c0__` 现代副本<br>⏸️ 待用户清除站点数据后 UAT 前台最终验证 | 用户反馈来源：直接反馈，无禅道编号；发版检查清单 5.5/6.4 已改为 Ribbon 核对项（attach-selection-rule 手动步骤作废）；按钮位置：UCI 平台限制 classic ribbon 固定入「更多命令」溢出菜单（Sequence/图标调优后位于溢出顶部+带图标，r3 版，用户已确认接受）；⚠️ **生产发版同样为非托管时旧 appaction 必现，上线核对清单需加「生产导入后手动隐藏/删除旧 appaction」步骤**（已加入 2.4.2）；2026-07-28 用户提出按记录状态显隐按钮（未生效只显示申请/待审批只显示审批拒绝/生效都不显示），经评估 UCI 平台限制不可行：经典 Ribbon 规则须同步返回且拿不到选中行数据，Power Fx 显隐必须注册进 App 命令组件库（不进 Solution、违反生产零手动）——决定**维持现状**（按钮常显 + JS 点击拦截提示：状态不一致/状态不匹配均有弹窗提示） |
| 867 | 【客户信用评估记录】如果存在相同【客户】的在途（没有审批结果状态）的评估记录，给出不予保存的阻断流程的警告信息提示「存在有重复客户评估记录，需核查！」 | 客户信用评估（`mcs_credit_record`） | 2026-07-27 | ✅ 已修复待验证 | `mcs_credit_record.js` onSave 中的重复客户在途校验于 2026-06-28 被整段注释「临时禁用」，本地源码与 DEV1 已部署 WebResource 一致均为禁用状态，导致相同客户存在在途（状态 9-14）评估记录时新建记录仍可保存，无任何阻断提示 | `Code/Customizations/WebResources/JS/mcs_credit_record.js`（去除注释，恢复在途重复客户校验） | ✅ 2026-07-27 DEV1 实测复现 Bug：客户 `LTC客户-1` 存在多条在途记录（如 SCO202607230001 状态 9），新建同客户记录直接保存成功生成 SCO202607260001，无阻断提示（测试记录已删除）<br>✅ 2026-07-27 去除注释恢复校验，JS 已部署 DEV1 并发布（51196 bytes，回读与本地逐字节一致，node 语法检查通过）<br>⏸️ 待用户 DEV1 验证：新建同客户（有在途记录）记录应弹出「存在有重复客户评估记录，需核查！」并阻断保存 | 无新增 D365 组件；⚠️ 验证时需关注 BPF 阶段推进是否触发 0x80060802（参照 mcs_fsm_data 同类问题） |
| 无 | 【融资管理】提交立项审批仍提示「当前不允许提交立项审批」（字段未上表单） | 融资管理（`mcs_fsm_data`） | 2026-07-25 | ✅ 已修复待验证 | `mcs_can_initiated`/`mcs_can_project`/`mcs_bppstatus`/`mcs_approve_type` 未放到表单上（导出 FormXml 确认仅有 mcs_bppstatuscode+mcs_fsm_status），JS `getAttribute` 返回 null 恒按 false/空处理：提交审批 can_xxx 校验永远拦截、BPF 前进门禁读不到审批标记 | `Code/Customizations/WebResources/JS/mcs_fsm_data.js`（新增服务端字段值缓存+getFieldValue 回退读取；提交按钮改为异步实时查询 can_xxx；applyStageControl 审批中锁定兼容 bppstatuscode） | ✅ 2026-07-25 已部署 DEV1 并发布（WebResource d1a14571）<br>⏸️ 待用户 DEV1 验证：刷新表单后提交立项审批可正常提交 | 用户反馈来源：直接反馈，无禅道编号；如有编号请补充 |
| 无 | 【融资管理】融资立项阶段点【提交立项审批】提示「当前不允许提交立项审批」 | 融资管理（`mcs_fsm_data`） | 2026-07-25 | ✅ 已修复待验证 | `mcs_can_initiated` / `mcs_can_project` 元数据默认值未配置（数据表定义 v1 应为「是」），新建记录实际为「否」，提交审批前置校验 `canInitiated !== true` 拦截 | `Code/Tools/D365ToolCommon/Metadata/MetadataFieldService.cs`（新增 SetBooleanDefaultValueAsync 公共方法）<br>`Code/Tools/MetadataTool/Services/EntityManager.cs`（int 默认值按字段类型路由布尔/选项集） | ✅ 2026-07-25 元数据默认值已设 mcs_can_initiated=1 / mcs_can_project=1 并发布实体<br>✅ 存量 5 条测试记录（FSM202607250002-0005+无编号）can_initiated/can_project 已修复为「是」（curl PATCH，204×5，回读确认）<br>⏸️ 待用户 DEV1 验证提交立项审批 | 用户反馈来源：直接反馈，无禅道编号；如有编号请补充 |
| 无 | 【融资管理】BPF 阶段推进到融资立项后需求融资管理 tab 字段仍只读；BPF 侧窗格字段可直接编辑 | 融资管理（`mcs_fsm_data`） | 2026-07-25 | ✅ 已修复待验证 | `applyStageControl` 只读 `mcs_fsm_status`，而用户通过 BPF 阶段条推进阶段时 BPF 活动阶段存在 BPF 实例中、不回写 `mcs_fsm_status`，两者脱节；BPF 侧窗格字段（header_process_ 控件）未做只读锁定 | `Code/Customizations/WebResources/JS/mcs_fsm_data.js`（阶段来源改为 BPF 优先+状态字段兜底、阶段双向同步、lockBpfFields 复用 credit_record 模式） | ✅ 2026-07-25 已部署 DEV1 并发布（WebResource d1a14571）<br>⏸️ 待用户 DEV1 验证：融资立项阶段 tab 字段可写、BPF 侧窗格字段只读、阶段与状态字段双向同步 | 用户反馈来源：直接反馈，无禅道编号；如有编号请补充 |
| 无 | 【融资管理】点击 BPF 阶段条/保存时报「阻止保存窗体的 Web 资源」无法保存（0x80060802） | 融资管理（`mcs_fsm_data`） | 2026-07-25 | ✅ 已修复待验证 | 表单挂有启用的 BPF「融资管理」（18c5e92c），BPF 阶段导航会触发平台内部保存；`onSaveValidate` 无条件 `preventDefault` 做异步重复校验，拦截了 BPF 保存，平台报错 0x80060802（微软文档明确：BPF 表单 onSave 调 preventDefault 会触发该错误） | `Code/Customizations/WebResources/JS/mcs_fsm_data.js`（onSaveValidate：来源字段均未变更时跳过 preventDefault+异步重复校验，直接放行） | ✅ 2026-07-25 已部署 DEV1 并发布（WebResource d1a14571）<br>⏸️ 待用户 DEV1 验证：BPF 阶段条点击推进/保存不再报错；新建改来源字段时重复校验仍生效 | 用户反馈来源：直接反馈，无禅道编号；如有编号请补充 |
| 1151 | 【成交条件基线库】风控管理员提交信息后，事业部海外营销公司风控债权部部长仅能看到本部门的成交条件基线库数据 | 成交条件样板库（`mcs_trade_stpayterm`） | 2026-07-24 | ✅ 已修复待验证 | 1. 审批人角色名已从「事业部军长」变更为「事业部海外营销公司风控债权部部长」，JS/Plugin 硬编码未同步；2. 审批人数据范围=本部门，看不到其他部门制定人创建的数据，无法审批 | `Code/Customizations/WebResources/JS/mcs_trade_stpayterm.js`（ROLES.APPROVER 改名）<br>`Code/Customizations/Plugins/TradeStPayTerm/Validation/TradeStPayTermValidationPlugin.cs`（RoleApprover 改名）<br>`Code/Customizations/Plugins/TradeStPayTerm/Sharing/TradeStPayTermSharePlugin.cs`（新增）<br>`Code/Tools/D365ToolCommon/Data/RecordSharingService.cs`（新增）<br>`Code/Tools/MetadataTool/Program.cs`（新增 test-tradestpayterm-share / share-tradestpayterm-pending 命令，create-tradestpayterm-testdata 补事业部）<br>`Code/Tools/sync-plugin-to-remote.py`（新增 SharePlugin 映射） | ✅ 2026-07-24 本地编译 0 错 0 警<br>✅ JS 已部署 DEV1 并发布（34962 bytes）<br>✅ DEV1 独立 Assembly `SanyD365.Plugins.TradeStPayTerm` 已注册 SharePlugin Step（Update PostOperation，Filter=mcs_status，PreImage=mcs_status+mcs_businessunit）<br>✅ DEV1 自动化测试 3/3 通过：提交共享 POA mask=3 / 重复提交幂等 / 无事业部拦截<br>✅ DEV1 存量待审批记录 0 条，无需补共享<br>✅ 远程 tx-windows 同步+编译通过（仅项目原有警告）<br>✅ PR #6044 已合并到 uat（merge commit `4bfaa210b0d`）<br>✅ 已用合并后 uat 重编译并更新 DEV1 主 Assembly `SanyD365.D365Extension.Sales`（ID `9d6ff315`）<br>✅ 主 Assembly 已注册 SharePlugin Step（ID `5b627677-3e87-f111-ab0e-6045bd1c0925`，Update PostOperation，Filter=mcs_status）+ PreImage（mcs_status+mcs_businessunit）<br>✅ DEV1 独立 Assembly `SanyD365.Plugins.TradeStPayTerm` 已注销（红线执行完毕；注册主 Assembly 类型前必须先注销，否则 PluginType 同名冲突 2601）<br>✅ DEV1 主 Assembly 回归测试 3/3 通过<br>⏸️ 环境配套：创建角色+部长加入 BuTeam（用户操作）<br>⏸️ 用户 DEV1 验证 → n8n 发布 UAT（McsPlugin+McsWebResource）<br>✅ 主清单 AllComponent_Peter_NoUAT 已补 SharePlugin Step（92→34个）；Image 确认为 Step 子组件随 Step 导出，无需单独加（平台报错证实）<br>✅ SharePlugin Step 已加入发版包 McsPlugin（用户 UI 操作，2744 个 Step）；待 n8n 重新发布 McsPlugin 到 UAT（首次发布 Assembly 带出了类型但 Step 未在包内未同步）<br>✅ 角色名按 UAT 实际创建改为英文（`LTC Risk Control Configuration Admin` / `LTC Regional Overseas Risk Director`）：分支 `uat-20260724-peter-tradestpayterm-role-en`，PR #6059 已合并（merge commit `313552a4dfc`）；JS 已部署 DEV1；DEV1 主 Assembly 已用合并后 uat 重编译更新（ID `9d6ff315`），回归测试 3/3 通过 | 已确认方案：①角色名调整（制定人「风控配置管理员」不变）；②提交（状态0→1）时 Plugin 按表单所选事业部（`mcs_businessunit`）查 `mcs_bu.mcs_buteamid`，将记录共享（Read+Write）给该 BuTeam，零新增配置；③存量待审批记录一次性补共享<br><br>**Git**：分支 `uat-20260724-peter-tradestpayterm-share-1151` 已推送（commit `ee69ff46dbe`，3 文件：ValidationPlugin 角色名、新增 SharePlugin、csproj）；待用户合并 PR 到 uat。注意：推送前发现远程落后 origin/uat 41 个提交，已先拉平再同步重编译，工作区其他文件为 EOL 差异未入库 |
| 1160 | 【成交样本库】选择"未生效成交条件基线库视图"下所有记录，点击【批量申请】发起审批后，需手动刷新才能看到状态更新为"待审批" | 成交条件样板库（`mcs_trade_stpayterm`） | 2026-07-25 | 📋 已关闭 | 现代命令栏（Command Designer）的 JS 运行在隔离沙箱上下文，`batchUpdateStatus` 成功后调用的 `window.location.reload()` 实际不生效（DEV1 实测复现：更新成功但列表不刷新、选中状态保留）。正确做法：按钮追加 SelectedControl 参数，JS 用 `selectedControl.refresh()` 局部刷新列表 | `Code/Customizations/WebResources/JS/mcs_trade_stpayterm.js`（apply/approve/reject 增加 selectedControl 形参，新增 `refreshGrid`，成功后 grid.refresh 兑底 reload）<br>`Code/Tools/DeployTool/AppActionDeployer.cs`（新增 `UpdateButtonParameters` 公共方法）<br>`Code/Tools/DeployTool/Program.cs`（新增 `update-appaction-params` 命令）<br>DEV1 3 个现代按钮 appaction（`cr0c0__mcs_trade_stpayterm_apply/approve/reject!cra80_SanyOverseasCRM!...`）参数由 `[{"type":23}]` 改为 `[{"type":23},{"type":12}]`（12=SelectedControl，由系统 Refresh 按钮参数反推锁定） | ✅ 2026-07-25 DEV1 实测复现 Bug（申请成功但列表不刷新）<br>✅ JS 已部署 DEV1 并发布（36289 bytes，回读一致）<br>✅ DEV1 3 个现代按钮参数已更新<br>✅ DEV1 实测修复生效：批量拒绝成功→待审批视图 2→0 行自动刷新；批量申请成功→未生效视图 13→11 行自动刷新（grid 局部刷新，非整页 reload）<br>✅ 测试数据 TC26072403/07 已用批量拒绝恢复为未生效<br>✅ 顺带验证 #1151 SharePlugin 拦截正常（泵路未维护 BU 团队提交被拦截）<br>✅ 环境配套：已创建 `CONCRETE MACHINERY BU/泵路海外营销公司-BuTeam`（10beac9a）、维护 `mcs_bu.mcs_buteamid`、把邱正卫加入团队（MetadataTool 新增 `setup-bu-team <mcs_buID> <用户domain>` 幂等命令）<br>✅ 2026-07-25 用户 DEV1 验证通过<br>⏸️ UAT 发布中：JS 已到 UAT（36440 bytes 逐字节一致）；实体小包（2 实体+1 流程+10 旧按钮）重新发布后，用户在 UAT Command Designer 为 3 个批量按钮手动追加 SelectedControl 参数（发版手册手动步骤） | 无新增 D365 组件；⚠️ 现代按钮不可随包（依赖 App 级共享命令组件库，跨环境导入必冲突，已实测失败）；生产发版同样需在发版手册写 Command Designer 手动步骤 |
| 874 | 【客户信用评估记录】跳转到下一步后，流程阶段没有自动定位到相应阶段，需要手动刷新表单才行（点 BPF 弹出框【下一阶段】，阶段条往下走，但弹出框仍显示上一阶段字段） | 客户信用评估（`mcs_credit_record`） | 2026-07-27 | ✅ 已修复待验证 | `mcs_credit_record.js` 的 `preventBpfGoBack` 在 PreStageChange(Next) 中 `preventDefault()` 拦截平台默认导航，改走自定义 `nextStep`→`updateStatus`（Web API 更新 `mcs_status`）→ 服务端 `BpfStageSyncPlugin` 同步 `activestageid` → `data.refresh(true)` 只重绘阶段条；BPF 弹出框（flyout）是绑定「点击时选中阶段」的客户端组件，数据刷新不会重绘，故仍显示上一阶段内容 | `Code/Customizations/WebResources/JS/mcs_credit_record.js`（新增 `reselectActiveBpfStage`+`clickBpfStageButton`；PreStageChange 增加 `_bpfNavigating` 防重入放行；`updateStatus`/`submitBppApproval`/`abandonBppApproval` 的 refresh 后接入重绘） | ✅ 2026-07-27 DEV1 实测复现 Bug（SCO202607270001：点下一阶段后弹出框停留「发起信用评估(已完成)」）<br>✅ 方案 A 第一版 `setActiveStage(同阶段)` 实测为 no-op，弹出框不重绘<br>✅ 第二版发现表单脚本运行在内容 iframe、BPF 阶段条/弹出框在顶层文档，`document.getElementById` 找不到元素<br>✅ 第三版改为 `window.top.document` 查找并点击阶段条按钮（按钮 ID=固定前缀 `MscrmControls.Containers.ProcessBreadCrumb-processHeaderStageButton_`+阶段 GUID），DEV1 轮询验证通过：点击后 ~0.9s 弹出框自动从「发起信用评估(已完成)」重绘为「关联客户代码（Coface Code 字段）」<br>✅ JS 已部署 DEV1 并发布（55122 bytes，回读与本地逐字节一致，node 语法检查通过）<br>✅ 3 条测试记录（SCO202607270001~0003）已删除<br>⏸️ 待用户 DEV1 验证：点 BPF 弹出框【下一阶段】后弹出框应自动显示新阶段字段，无需手动刷新 | 无新增 D365 组件，无需加主清单；截图 `bug874-fixed.png`；⚠️ 工具栏【进入下一阶段】推进时若弹出框未打开则不会主动弹出（flyoutOpen 检测），仅修复「弹出框已打开但内容过期」场景 |
| 无（关联 #1282） | 【成交条件基线库】客户分类代码等必填字段可通过 Excel 导入绕过；非必填维度空值按通配处理未真正参与重复校验 | 成交条件样板库（`mcs_trade_stpayterm`） | 2026-07-27 | ✅ 已修复待验证 | 1. 事业部/客户分类代码/首付款比例/账期/付款频次仅为 ApplicationRequired（表单客户端强制），Excel 导入/API Create 不经过表单可写入空值，Plugin 无非空兜底；2. 重复校验 IsWildcardMatch/IsMultiSelectMatch 把空值当通配（任一方空即视为匹配），空维度记录与任意记录互判重复、且未实现「空仅与空相同」的精确语义 | `Code/Customizations/Plugins/TradeStPayTerm/Validation/TradeStPayTermValidationPlugin.cs`（新增 ValidateRequiredFields 必填兜底；重复校验空值语义改为「空/NA 归一化后仅与空相同」）<br>`Code/Tools/MetadataTool/Program.cs`（新增 `test-tradestpayterm-required` 测试命令） | ✅ 2026-07-27 本地编译 0 错 0 警<br>✅ DEV1 临时 Assembly 注册（Type 加 _Temp 后缀避 2601）+ Create/Update PreOp Step<br>✅ DEV1 自动化测试 13/13 通过（测试期间临时停用主 Assembly Validation 2 个 Step，测后已恢复启用）：TC1 基线创建成功；TC2-6 缺客户分类/事业部/首付款/账期/频次逐项拦截；TC7 全维度含空相同判重复；TC8 子公司空 vs 非空不判重复；TC9 非空精确判重复；TC10 客户等级空 vs A0 不判重复；TC11 子公司 NA 归一为空判重复；TC12 Update 改首付款不误伤；TC13 Update 清空客户分类拦截<br>✅ 测试记录已清理；DEV1 临时 Assembly 已注销（Step×2/Type/Assembly 全清，红线执行完毕；首次注销遇死锁 1205 重试成功）<br>✅ 已同步 tx-windows（sync-plugin-to-remote.py）并远程编译通过（仅项目原有警告；csproj BOM 残留已丢弃）<br>✅ 分支 `uat-20260727-peter-tradestpayterm-required` 已推送 Azure DevOps（commit `0311c69ecad`，仅 Validation 单文件 +174/-27，commit 关联 #1282；远程工作区已切回 uat）<br>⏸️ 待用户 Azure DevOps 创建 PR 合并到 uat → 拉取合并后 uat 重编译 → 更新 DEV1 主 Assembly → 主 Assembly 回归<br>✅ PR #6179 已合并到 uat（merge commit `c2475c5bf8c`，用户操作）<br>✅ 已拉取合并后 uat 重编译（0 错误）并更新 DEV1 主 Assembly `SanyD365.D365Extension.Sales`（ID `9d6ff315`，8403 KB，无 PluginType 差异）<br>✅ 主 Assembly 正式链路回归 13/13 通过（--main 模式，主 Assembly Step 保持启用）：必填五项逐项拦截 / 全维度含空判重复 / 空 vs 非空不判重复 / NA 归一为空判重复 / Update 不误伤 / Update 清空客户分类拦截（测试记录已清理）<br>⏸️ 待用户 DEV1 验证 → n8n 发布 McsPlugin 到 UAT | 用户反馈来源：直接反馈（截图必填项+禅道 #1282 需求原文「考虑字段空情况」），无新禅道编号；修复后存量空客户分类记录更新时将被必填拦截，需提示用户 |
| 无 | 成交条件基线库表单：重新修改大区/子公司后事业部未重新带出 | 成交条件样板库（`mcs_trade_stpayterm`） | 2026-07-24 | ✅ 已修复待验证 | 1. JS 缺少「大区→事业部」带出逻辑；2. 表单设计器给大区配置了按事业部筛选（`mcs_mcs_bu_mcs_region_buid`），事业部有值后大区被锁在当前事业部范围；3. `validateLookupParent` 用 SDK 属性名 `mcs_buid` 读 WebAPI 返回值（应为 `_mcs_buid_value`），属死代码未接线 | `Code/Customizations/WebResources/JS/mcs_trade_stpayterm.js` | ✅ 2026-07-24 已部署 DEV1 并发布（34929 bytes，回读一致）；双向联动：大区变更→带出事业部，事业部变更→校验并清空不匹配大区；待用户 DEV1 验证 | 用户反馈来源：直接反馈，无禅道编号；如有编号请补充 |

| 1282 | 如果存在相同维度（事业部、子公司、国家、产品分类、客户分类、客户等级）的成交条件标准记录重复场景（考虑字段空情况），给出不予保存的阻断流程的警告信息提示"存在有重复记录，需核查！" | 成交条件样板库（`mcs_trade_stpayterm`） | 2026-07-27 | 📋 已关闭 | **不是只有 JS 拦截（JS 根本没有重复校验）**；后端 `TradeStPayTermValidationPlugin.ValidateDuplicate`（Create/Update PreOp，DEV1 Step 已注册）有重复校验且正常工作，但校验锚点是 `mcs_buid`（事业部编码字符串），开头 `if (string.IsNullOrEmpty(buId)) return;` 直接跳过；Excel 导入只映射 Lookup `mcs_businessunit`+名称，`mcs_buid` 靠表单 JS `autoFillParentLookup` 带出、导入时 JS 不执行 → buid 为空 → 整个重复校验被旁路。附加问题：存量导入记录 buid 全空，查询条件 `mcs_buid eq code` 也匹配不到它们 | `Code/Customizations/Plugins/TradeStPayTerm/Validation/TradeStPayTermValidationPlugin.cs`（ValidateDuplicate：buid 空时从 mcs_businessunit lookup 反查 mcs_bu.mcs_code 作锚点并回填；新增 GetLookupValue/ResolveBuCode；查询条件改 mcs_buid eq code OR mcs_businessunit eq lookupId） | ✅ 2026-07-27 后台 API 实测（Xrm.WebApi.createRecord，绕开表单 JS）：<br>✅ 修复前测试A（对照组，buid=BU-1018 有值+维度与 TC26071202 完全重复）→ 被拦截「保存失败: 存在有重复记录，需核查！」<br>✅ 修复前测试B（模拟导入，不传 buid 仅传 businessunit lookup+相同维度）→ 重复记录创建成功（Bug 复现，数据已删）<br>✅ 确认 `mcs_bu.mcs_code`=事业部编码（如 BU-1018）<br>✅ 修复后 DEV1 独立 Assembly 回归 4/4：A 对照组拦截 ✅；B 导入式重复→拦截 ✅（原 Bug 场景）；C 导入式非重复→创建成功且 buid 自动回填 BU-1018 ✅；D 对存量空 buid 记录（TC26072411，帕尔菲格 lookup）重复→经 OR-lookup 条件拦截 ✅<br>✅ 本地编译 0 错 0 警；DEV1 独立 Assembly 已注销（红线执行完毕）<br>✅ 已同步 tx-windows 并单文件上传（scp 限流，整脚本 5 次失败改单文件方案）<br>❌ 远程编译被同事提交阻塞：HEAD 祖先提交 `eb0dac609f8`（2026-07-27 10:18「合同审批通过同步oms」）调用不存在的方法 `IContractRepository.QueryRegionAndBuById`，CS1061，与本次改动无关<br>✅ 用户指示拉取最新：git pull 后（HEAD `31eca9d6975` PR 6131）远程编译通过，无 error<br>✅ Git 已授权并推送：分支 `uat-20260727-peter-tradestpayterm-dup-1282`，commit `b9418978c39`（仅 Validation 单文件 +87/-10，commit 关联 #1282）；远程仓库已切回 uat<br>✅ PR #6133 已合并到 uat（merge commit `439b03f6a75`，用户操作）<br>✅ 已用合并后 uat 重编译（BUILD_OK 无 error）并更新 DEV1 主 Assembly `SanyD365.D365Extension.Sales`（ID `9d6ff315`，8392 KB）<br>✅ 主 Assembly 后台回归 3/3：A buid 有值重复→拦截 ✅；B 导入式无 buid 重复→拦截 ✅（原 Bug 场景）；C 导入式非重复→创建成功且 buid 自动回填 BU-1018 ✅（测试数据已清理）<br>✅ 用户要求复核（最贴近 Excel 导入：只传国家/产品分类名称文本，不传 GUID/buid）：T1 名称导入式重复（南非+燃油牵引车+A0）→ 拦截「存在有重复记录，需核查！」✅；T2 名称导入式非重复（Argentina+电动牵引车+A1）→ 放行且名称→GUID 解析+buid 回填正常 ✅（测试数据已清理）<br>⏸️ 待用户 DEV1 验证：同一 Excel 二次导入应被阻断并提示「存在有重复记录，需核查！」<br>✅ 2026-07-27 UAT 后台实测（用户已 n8n 发布 McsPlugin 到 UAT）：完全相同的导入式数据（泵路+南非+燃油牵引车+A0，无 buid 仅 lookup）创建第 1 条成功且 buid 自动回填 BU-1018（TC2607270025，新代码行为铁证），创建第 2 条被拦截「保存失败: 存在有重复记录，需核查！」→ **UAT 代码已同步**，测试数据已清理<br>✅ 顺带发现 UAT 存量重复对（TC2607270001/TC2607240023 巴西+泵车+A2、TC2607270002/TC2607240024 阿塞拜疆+拖泵+A3，均为修复前导入），如需清理请告知<br>工具：MetadataTool 新增通用 `create-record <实体名> <JSON|@文件>` 命令（类型后缀 #int/#decimal/#optionset/#optionsetcollection/#lookup，本地工具不涉及 D365 组件） | 重现：同一 Excel 模板二次导入仍成功（期望阻断提示重复）；修复方案已报用户待授权：buid 空时从 mcs_businessunit lookup 解析 mcs_bu.mcs_code 作锚点并回填，查询条件改 `mcs_buid eq code OR mcs_businessunit eq lookupId` 覆盖存量空 buid 记录 |
| 1283 | 厂端授信额度申请单表 信息提交后，信息表处于 审批中，信息仍然能修改（调整厂端授信额度，调整原因），应该只读 | 厂端授信（`mcs_fca_quotaapp`） | 2026-07-27 | ✅ 已修复待验证 | `mcs_fca_quotaapp.js` 完全没有按审批状态锁定字段的逻辑（grep 无 setDisabled），提交审批（mcs_bppstatus=2）后表单全部字段仍可编辑；且 `mcs_bppstatus` 未放在表单上，getAttribute 读不到状态 | `Code/Customizations/WebResources/JS/mcs_fca_quotaapp.js`（新增 applyBppStatusLockAsync/lockFormIfApproving/lockAllFields：onLoad 对已有记录从服务端读 mcs_bppstatus，审批中(2)/通过(3) 全表单只读；submitToBpp 提交成功后立即锁定，因 data.refresh 不重触发 onLoad） | ✅ 2026-07-27 部署前导出 DEV1 现有 WebResource 与本地 diff，确认差异仅限本次改动（防覆盖他人修改）<br>✅ JS 已部署 DEV1 并发布（39122 bytes，回读与本地逐字节一致，node 语法检查通过）<br>✅ DEV1 三状态实测：审批中 FCA202607130001 全字段 disabled=true；通过 FCA202607130002 全字段 disabled=true；申请(撤回) FCA202607130003 字段可编辑 disabled=false<br>⏸️ 待用户 DEV1 验证：审批中/通过记录表单只读，驳回/撤回后可编辑重新提交 | 无新增 D365 组件，无需加主清单；截图 `bug1283-fixed.png`；锁定范围经用户确认=审批中+通过都锁；仅前端锁定（与融资管理模块一致），如需防 API 绕过可后续加后端 Plugin |
| 1271 | 厂端授信额度申请单表审批传BBP信息有问题（客户编码为空，客户名称写成了客户编码） | 厂端授信（`mcs_fca_quotaapp`）BPP | 2026-07-27 | ✅ 已修复待验证 | 1. `BPPHandlerServiceForFcaQuotaApp.GetBppFormData` 把 `mcs_accountid`（实际指向 `mcs_customermasterdata`）当作 `account` 查询，必然查不到（UAT 实测 404 `0x80040217`）导致客户编码为空；2. 实体 `mcs_custname` 字段按 JS 设计存放的是客户编码（`mcs_sapnumber`），BPP 直接透传导致「客户名称」显示成编码 | `Code/SanyD365Project/Service/SanyD365.Main/Entities/BPP/BPPHandlerServices/BPPHandlerServiceForFcaQuotaApp.cs`（改为直接用 `mcs_accountid` 查 `mcs_customermasterdata`，取 `mcs_sapnumber`→客户编码、`mcs_name`→客户名称，兑底 Lookup 显示名→记录原值） | ✅ 2026-07-27 UAT 只读模拟验证（记录 FCA202607260002 / `633bef6f-da88-f111-8077-6045bd1f12e5`）：旧链路查 `account(937bfcd2-...)` 返回 404（根因实锤）；新链路查 `mcs_customermasterdata` 返回 `mcs_sapnumber=0214003831`、`mcs_name=测试报价客户`，与预期完全一致<br>✅ 代码已同步 tx-windows（CRLF 一致，回读逐字节一致）<br>✅ 远程 `SanyD365.Main.csproj` Release 编译 0 错误（仅项目原有警告）<br>✅ 分支 `uat-20260727-peter-fca-bpp-custname-fix` 已推送 Azure DevOps（commit `9ccb21d549e`，仅 1 文件 +22−21；远程工作区已切回 `uat`）<br>✅ PR !6120 已合并到 `uat`（merge commit `6635713a0ac`）<br>✅ 远程已拉取合并后 `uat` 重新编译 `SanyD365.Main`，0 错误<br>⏸️ 待用户 n8n 发布 Messagehandler/ClientAPI 到 UAT 后端到端验证 | 何慧丽 2026-07-27 报告，UAT 记录 FCA202607260002 复现；无新增 D365 组件 |

| 1307 | 在【不予授信客户】中人工勾选场景1（征信黑名单）或场景2（近3年≥100万实质坏账），授信额度不为0 | 厂端授信（`mcs_fca_proc`） | 2026-07-27 | ✅ 已修复待验证 | `mcs_fca_proc.js` `handleCreditCheckNext` 按 PRD「人机协同」只拦截系统自动判断的场景 3/4，人工勾选 1/2 时点【下一步】不拦截、不回写主数据不予授信标志，可一路推进到生效启用并算出额度（PRD 仅明确 3/4 拦截，属设计模糊点，业务确认 1/2 也需拦截） | `Code/Customizations/WebResources/JS/mcs_fca_proc.js`（拦截条件由「含 3/4」扩为「含任意场景 1/2/3/4」） | ✅ 2026-07-27 部署前导出 DEV1 现有 WebResource 与本地 diff，确认差异仅限本次改动（防覆盖他人修改）<br>✅ JS 已部署 DEV1 并发布（38238 bytes，回读与本地逐字节一致，node 语法检查通过）<br>✅ DEV1 实测：新建记录选 LTC客户-1 + 勾选场景1，点下一阶段停留授信校验，主数据 `mcs_creditgrant` 回写为 true（modifiedon 2026-07-27T05:34Z 证实）<br>✅ 2026-07-27 用户 DEV1 验证通过<br>⏸️ 待 UAT 发布（McsWebResource 包）后用户 UAT 验证 | 修复=人工勾选任一场景即拦截（复用 `blockAndUpdateCustomerReject`：停留授信校验+回写主数据【不予授信客户】=是）；拦截后进不了模型计算，额度自然为 0；无新增 D365 组件，无需加主清单 |
| 无 | 【厂端授信模型计算表】授信校验阶段已过后【不予授信】仍可勾选，可绕过拦截走到生效启用（#1307 衍生） | 厂端授信（`mcs_fca_proc`） | 2026-07-27 | 🔧 修复中 | `mcs_fca_proc.js` `applyReadOnlyByStatus` 只在计算状态=生效启用(3)才锁全表单，模型计算阶段(2)【不予授信】字段仍可编辑，此时勾选场景 1/2 后点【下一步】无任何拦截（`handleCreditCheckNext` 只在授信校验阶段触发），可继续走到生效启用 | `Code/Customizations/WebResources/JS/mcs_fca_proc.js` | ✅ 2026-07-27 JS 已部署 DEV1 并发布（40523 bytes，回读与本地逐字节一致，node 语法检查通过）<br>⏸️ 待用户 DEV1 验证：①授信校验阶段【不予授信】可编辑；②进入模型计算后字段锁定不可选；③存量在途记录（已选场景+模型计算阶段）点【下一步】被兑底拦截 | 修复=①新增 `applyCreditRejectLock`：仅 BPF 活动阶段=授信校验时【不予授信】可编辑，进入模型计算及以后锁定；②`handleModelCalcNext` 兑底：已选不予授信场景的记录阻止生效启用 |
| 无 | 【厂端授信模型计算表】模型版本过期/未生效后仍可执行模型计算并回写额度（版本校验未前置） | 厂端授信（`mcs_fca_proc`） | 2026-07-27 | ✅ 已修复待验证 | `FcaProcCalculationPlugin` 版本选择排在计算完成后（第 7 步），且 `ModelVersionService.GetActiveVersion()` 查不到有效版本时静默返回 null 不中断，三因子额度照常回写；模型参数（`mcs_fca_mdlconfig`）查询也无生效/日期过滤 | `Code/Customizations/Plugins/FactoryCredit/Calculation/FcaProcCalculationPlugin.cs`（版本校验前置为步骤 0，无有效版本抛 `InvalidPluginExecutionException` 中断计算；后续步骤复用已查版本不再重查）<br>`Code/Customizations/WebResources/JS/mcs_fca_proc.js`（新建表单检测无有效模型版本则弹窗提示并 onSave 阻止保存，复用已有语言 key `FcaProc_NoActiveModelVersion`）<br>`Code/Tools/FactoryCreditTest/Program.cs`（新增测试命令 `set-version-active`） | ✅ 部署前导出 DEV1 JS 与本地 diff，差异仅限本次改动（防覆盖他人修改）<br>✅ JS 已部署 DEV1 并发布（WebResource e2add232，node 语法检查通过；本地插件编译 0 错 0 警）<br>✅ 场景①后端：版本 V20260830 停用后 API 触发计算（LTC客户-1），被拦截报错「模型计算失败: 未找到生效且处于有效期内的模型版本，请先维护【厂端授信模型版本】后再执行模型计算。」（0x80040265），测试记录已删<br>✅ 场景①前端：新建表单打开即弹窗提示；填客户后点保存被拦截并再次弹窗，记录保持未保存（截图 `fca-guard-create-blocked.png`）<br>✅ 场景②：版本恢复生效后 API 触发计算成功，额度 10,225,319.26 正常回写、版本 V20260830/模型描述正常盖章；新建表单不再弹窗；测试记录已删<br>✅ 独立 Assembly `SanyD365.Plugins.FactoryCredit` 已注销（Step+Type+Assembly 全清，红线执行完毕）；DEV1 仅剩主 Assembly 2 个原有 Step；临时测试子类文件已删除<br>✅ 已同步 tx-windows（同步前 diff 确认差异仅限本次改动；PluginBase 框架转换复用 sync-plugin-to-remote.py）<br>✅ 远程 Release 编译 0 错误（仅项目原有警告）<br>✅ 分支 `uat-20260727-peter-fca-versionguard` 已推送 Azure DevOps（commit `30a6dbb06a5`，仅 1 文件 +13/-9；远程工作区已切回 uat）<br>✅ PR #6170 已合并到 uat（merge commit `92869cc4529`，用户操作）<br>✅ 已用合并后 uat 重编译（0 错误）并更新 DEV1 主 Assembly `SanyD365.D365Extension.Sales`（ID `9d6ff315`，8400 KB，无 PluginType 差异）<br>✅ 主 Assembly 回归 2/2：版本停用→API 触发计算被拦截报「未找到生效且处于有效期内的模型版本」；恢复生效→计算成功、额度/版本/描述正常回写（测试记录均已删，V20260830 已恢复生效）<br>⏸️ 待用户 n8n 发布 UAT（McsPlugin + McsWebResource）后 UAT 验证 | 用户反馈来源：直接反馈（模型版本 V20260830 有效期讨论），无禅道编号；新建拦截方式经用户确认=阻止保存；后端 Plugin 为兑底（防 API 绕过）；⚠️ DEV1 验证时因主 Assembly 已有同名 Type（2601 冲突），曾以临时子类 `FcaProcCalcGuardTestPlugin` 注册验证，验证后已注销删除；无新增 D365 组件（正式落地走主 Assembly 更新，不加主清单） |

补充验证（2026-07-27 14:25）：#1307 期望 2 已实证（LTC客户-1 主数据 `mcs_creditgrant=true`，modifiedon 05:34Z）；期望 3 逻辑兑底（拦截后 plugin 不执行，额度保持默认 0）。FCM202607270002（13:46 创建，reject=1 却走到生效启用）排查为**浏览器缓存旧 JS**（第二版修复 13:42 才发布，用户未强刷），非修复失效；衍生修复待用户强刷后 DEV1 验证。

---

## 登记模板（新 Bug 复制此行）

```markdown
| 禅道编号 | 标题 | 模块 | YYYY-MM-DD | 🔧 修复中 | （根因，修复后填） | （改动文件） | （部署/验证记录） | （方案/备注） |
```

| 1336 | 在客户上面有 净资产 有值，厂端授信模型计算取值为0，协助确认原因 | 厂端授信（`mcs_fca_proc`） | 2026-07-28 | ✅ 已修复待验证 | UAT 实证：客户 Kedai Kek（0214006316）有 47 条 active 的 NetAssets 客户标签，其中 29 条复核定量值 `mcs_itemintvalue2` 为空（Coface 历史同步生成，复核值仅首次赋值）；`ThreeFactorCalculationService.GetNetAssets` 查询 TopCount=1 无排序、不过滤 isscore，恰好命中空值历史标签 → 复核值 null 按 0 处理，财务能力=0×0.9=0；且 Found=true 不触发历史基准额度兜底。客户画像页签过滤 `mcs_isscore=1` 且显示文本字段（空值回退显示集成值），故画像显示 11361465.84 正常 | `Code/Customizations/Plugins/FactoryCredit/Calculation/Services/ThreeFactorCalculationService.cs`（GetNetAssets：与客户画像逻辑对齐——加 `mcs_isscore=true` 过滤 + `modifiedon` 倒序取最新 + `mcs_itemintvalue2` NotNull 兜底；取不到有值标签则 Found=false 走历史基准额度兜底） | ✅ 2026-07-28 本地编译 0 错 0 警<br>✅ 2026-07-28 DEV1 独立 Assembly（临时子类 FcaProcCalcNetAssetsTestPlugin 避 2601）双场景实测通过：TC1 核心场景——LTC客户-1 新建更新的 isscore=1 空复核值陷阱标签后触发计算，仍取到有值标签，日志「净资产 x 系数1 = 11,361,465.84 x 0.90 = 10,225,319.26」，MAX 初始额度=10,225,319.26；TC2 兜底场景——新建仅含空复核值标签的测试客户，计算走历史基准额度兜底=1,000,000.00 且日志为兜底文案<br>✅ 测试数据（3 条 proc/2 条 tag/1 account/1 主数据）已全删；独立 Assembly 已注销（Step×2/Type/Assembly 全清，红线执行完毕）；主 Assembly 2 个 Step 已恢复启用（测试期间临时停用防双触发）<br>✅ 已同步 tx-windows（diff 确认差异仅限本次修复 +8/-1）<br>✅ 远程 Release 编译 0 错误（仅项目原有警告）<br>✅ 分支 `uat-20260728-peter-fca-netassets-1336` 已推送 Azure DevOps（commit `a899a277754`，仅 1 文件 +8/-1，commit 关联 #1336；远程工作区已切回 uat）<br>⏸️ 待用户 Azure DevOps 创建 PR 合并到 uat → 拉取合并后 uat 重编译 → 更新 DEV1 主 Assembly 回归 → n8n 发布 UAT<br>✅ PR 已合并到 uat（用户操作，2026-07-28）<br>✅ 已拉取合并后 uat 重编译（0 错误）并更新 DEV1 主 Assembly `SanyD365.D365Extension.Sales`（ID `9d6ff315`，8403 KB，无 PluginType 差异）<br>✅ 主 Assembly 正式链路回归通过：重建 isscore=1 空复核值陷阱标签后触发计算，日志「净资产 x 系数1 = 11,361,465.84 x 0.90 = 10,225,319.26」，初始额度=10,225,319.26（测试 proc/标签已删）<br>✅ 2026-07-30 用户 n8n 发布 McsPlugin 到 UAT 完成；UAT 回归通过：融资需求阶段记录（FSM202607270001）提交立项审批被拦截报「当前融资状态不允许提交该类型审批…」，事务完全回滚（modifiedon 未变、字段全空），Trace 实锤主 Assembly 新代码生效；FSM202607290001 保持真实审批中不受影响（UAT 客户 Kedai Kek 重算应取 11361465.84） | 何慧丽 UAT 反馈，FCM202607270011 复现；取数规则经用户确认=与画像一致（isscore=1+最新+有值兜底）；UAT 现场数据：2026-07-27 12:32 计算命中空值标签取 0；PRD 未明文规定多条标签取值规则（需求缺口，参照画像 PRD「多条取更新日期最新」）；修复后无需处理存量空值标签（查询自动跳过） |
| 无 | 【客户信用评估记录】UAT 人工复核后进入信用分计算报错「信用分计算发生系统错误…does not have ReadAccess right(s) for record with id 31…」 | 客户信用评估（`mcs_credit_record`）信用分计算 | 2026-07-28 | 🔧 修复中 | `CreditScoreCalculationPlugin` 全程使用调用用户上下文（`CreateOrganizationService(context.UserId)`），第二步 `Retrieve("account")` 读取评估单关联客户（UAT 实证=Kedai Kek `31ac184d-dc12-f111-8341-00224858a453`，与报错记录 ID 前缀一致）时，操作员 UATUser06（子总/事业部风控，account Read=本部门及下级部门）对该客户无记录级访问权 → 计算中断；后续 `mcs_customermasterdata`/`salesorder`/评分卡/标签读取存在同类隐患 | `Code/Customizations/Plugins/CreditScore/Plugin/CreditScorePlugin.cs`（计算所需的关联数据读取改用系统上下文 `CreateOrganizationService(null)`：account/客户主数据/销售订单/评分卡/评分项目/客户标签；评估记录本身读取与最终写回保留用户上下文）<br>`Code/Customizations/Plugins/CreditScore/Calculator/ScoreCalculator.cs`（构造函数拆分为 readService/writeService，读走系统上下文、标签得分回写保留用户上下文） | ✅ 2026-07-28 本地编译 0 错 0 警<br>✅ 2026-07-28 DEV1 独立 Assembly 验证：注册独立 Assembly+Step 后将 SCO202607230001 状态 12→13，信用分=70 正常算出、58 项标签得分回写正常（score 字段有值）<br>✅ DEV1 独立 Assembly 已注销（Step/Type/Assembly 全清，红线执行完毕）；测试记录 SCO202607230001 因状态流转校验拦截（13→12 非法）停留在 13(在途)，信用分 70<br>✅ 发现远程 ScoreCalculator.cs 领先本地 4 处（OverdueModel 比例计分/listvalue 取 mcs_credititem_value.mcs_listvalue/中文枚举归一化/SaveTagScores 按 TagId 合并取最高分且失败抛错），已合并保留，本地已反向同步<br>✅ 已上传 tx-windows（diff 确认仅本次改动：Plugin +10/-3、Calculator +18/-8），远程 Release 编译 0 错误（仅项目原有警告）<br>✅ 分支 `uat-20260728-peter-creditscore-read-syscontext` 已推送 Azure DevOps（commit `c4e86e645b9`，仅 2 文件 +24/-12，无禅道编号；远程工作区已切回 uat）<br>✅ PR #6237 已合并到 uat（merge commit `ad8a3405f14`，用户操作）<br>❌ 拉取合并后 uat 重编译被同事提交阻塞：`IAppCreditLimitApplication.cs` 第 70 行接口方法声明漏分号（error CS1002，来源提交 `d7c66ae79a8`「fix:飞书消息」），与本次改动无关；经用户决定等同事修复后再拉取<br>✅ 同事已修复编译（PR #6238），已拉取最新 uat（`d25bfe5a3eb`）重编译 0 错误（DLL 8410 KB）<br>✅ 已更新 DEV1 主 Assembly `SanyD365.D365Extension.Sales`（ID `9d6ff315`）<br>✅ DEV1 主 Assembly 正式链路回归通过：SCO202607130001 状态 11→12→13，信用分=72 正常算出、标签得分回写无报错（远程合并版 SaveTagScores 失败会抛错，无报错即回写成功）；记录停留在 13(在途)<br>✅ 2026-07-28 用户 n8n 发布 McsPlugin 到 UAT（同包含 PR #6237 信用分修复 + PR #6261 Coface 基础数据系统上下文读取）<br>✅ UAT 实测（SCO202607200001）：12→11 数据集成刷新 SUCCESS（财务指标对照/NACE/汇率系统上下文读取正常，净资产标签 23:37 刷新）；12→13 信用分计算=70 与历史一致，不再报 ReadAccess 权限错；记录停在 13(在途)，UATUser06 可继续推进审核申请<br>⏸️ 待 UATUser06 页面操作确认（评分结果可见、可进入审核申请）后关闭 | 用户 UAT 反馈（UATUser06 / SCO202607200001），无禅道编号；方案 A 经用户确认；⚠️ 标签得分回写若操作员对跨部门标签无写权限仍会被吞掉（原有行为，trace 可查） |
| 1408 | 厂端授信动态运用 涉及厂端授信余额调整接口逻辑修改及字段添加 | 厂端授信（`mcs_fca_quota` / Custom API `mcs_AdjustFcaQuotaBalance`） | 2026-07-29 | ✅ 已修复待验证 | 与最新版 PRD（动态运用 PRD V1.0 §5.3）逐条比对：接口逻辑（占用/释放/防重/台账 adjustamt=0/允许负余额）均已一致；两个差异点经用户裁决维持现状——①初始化不清零占用（决策点 A，PRD 3.2 历史占用走"占用"动作补录佐证）②环节编码维持台账表 1-11。实际缺口：`mcs_usedsellerbalance` 缺 2052 中文标签、未上主表单 | `Code/Tools/MetadataTool/Program.cs`（新增 `add-form-field` 命令，复用 EntityManager.UpdateMainForm 公共方法）<br>DEV1 元数据：`update-field-displayname` 补中文标签 + 字段加主表单 + 发布实体 | ✅ 2026-07-29 DEV1 元数据修复完成：中文标签「厂端授信占用金额USD」已补、字段已上主表单（9 字段）、实体已发布<br>✅ 2026-07-29 DEV1 接口回归 4 场景通过（客户 BMW0001 + 合同 LTC-20260708075141 + 订单 SID202605270001）：①初始化(环节1) 占用不清零、余额=新额度-占用 ✅；②占用(环节6) 1000 → 余额 10224319.26、占用+1000 ✅；③重复占用拦截「对应合同已执行占用」✅；④释放(环节8) → 余额回到 10225319.26、占用归 0 ✅；不变式 额度=余额+占用 全程成立<br>✅ 6 条测试台账（FCR2026072900001-0006）已清理，额度记录已恢复原值<br>⏸️ 待用户 DEV1 验证表单显示<br>✅ 2026-07-29 用户反馈表单控件无属性+显示英文，根因二重：①`UpdateMainForm` 生成的单元格 XML 缺 locklevel/colspan/rowspan 且只有单语标签，新版设计器无法解析属性面板；②`update-field-displayname`（UpdateAttributeRequest）对 Money 字段静默失败，2052 标签未写入，设计器重放字段时把英文复制到 1033/2052 两个 Form 级标签<br>✅ 修复（经用户批准扩展公共方法）：`UpdateAttributeDisplayName` 改走 `MetadataFieldService.SetFieldDisplayNameAsync`（Web API PUT）→ 字段 2052 标签补齐；`UpdateMainForm` 单元格补齐 locklevel/colspan/rowspan+双语标签（后续插入设计器兼容）；新增 `EntityManager.UpdateFormFieldLabel` 公共方法 + CLI `update-form-field-label` → Form 级 2052 标签改为中文并发布<br>✅ 2026-07-29 Playwright 实测 DEV1 表单：中文界面显示「厂端授信占用金额USD」$0.00 正常；英文界面回退字段级 1033 显示名（平台对 SDK 写入的 Form 级 1033 标签做规范化剥离，已接受，有字段显示名兜底双语均正确） | 陈东忍 2026-07-29 创建并指派邱正卫；接口代码不改；⚠️ 教训：初始化动作入参 mcs_usebalance=新额度值，测试传 0 会把额度抹 0（已当即恢复）；无新增 D365 组件（字段已存在），无需加主清单 |
| 1403 | 历史回款能力 月度在外货款 取数逻辑修正 | 厂端授信（`mcs_fca_proc` 模型计算三因子） | 2026-07-29 | 📋 已关闭 | PRD 要求按【生成版本】YYYYMM 取 12 个月、同月多条记录按月求和；DEV1 核实 `mcs_outstanding` **无【生成版本】字段**（仅 mcs_createon 源创建时间/mcs_basedate 基准日，该实体非我方负责，经用户裁决继续用 mcs_createon）；现有代码全量累加÷12 未按 PRD 表述按月分组求和（数学等价但需对齐口径） | `Code/Customizations/Plugins/FactoryCredit/Calculation/Services/ThreeFactorCalculationService.cs`（GetAverageMonthlyPayment：按 mcs_createon 的 yyyy-MM 分组逐月求和后累计÷12，Trace 输出各月明细）<br>`Code/Tools/FactoryCreditTest/TestMonthlySum.cs`（新增测试辅助）<br>`Code/Tools/FactoryCreditTest/Program.cs`（新增 monthly-sum-setup/monthly-sum-cleanup/query-audit 命令） | ✅ 2026-07-29 本地编译 0 错 0 警<br>✅ 2026-07-29 DEV1 独立 Assembly（临时子类 FcaProcCalcMonthlySumTestPlugin 避 2601）验证通过：造 3 条测试数据（2026-06 两条 600000+400000、2026-05 一条 200000），Trace 实测 `2026-06 月回款合计(RMB): 1000000.00`（同月求和✅）、`记录数: 3, 覆盖月份数: 2, 累计RMB: 1200000`、月均 100000 CNY / 7.00305 = 14279.49 USD ×系数2(0.8) = 历史回款能力 11423.59 USD（proc 计算日志一致）；模型额度 10,225,319.26 与 #1336 基线一致无回归<br>✅ 测试数据（proc×2/outstanding×6）已清理；造数并发事故致原 outstanding 备份丢失，已通过 audit 审计日志完整还原（新 ID `4185d194`，逾期金额 0/在外货款 500,000 与原值一致；原记录 mcs_createon 本就为 null 不参与计算；仅 GUID 不可复原）<br>✅ 独立 Assembly 已注销（Step/Type/Assembly 全清，红线执行完毕）；主 Assembly 2 个 Step 已恢复启用；临时子类文件已删<br>✅ 2026-07-29 已同步 tx-windows（sync 脚本被 CofaceDataSyncPlugin 历史校验失败阻断，改单文件手动转换：命名空间替换+CRLF，远程 diff 确认仅本次改动 +17/-4）<br>✅ 远程 Release 编译 0 错误（仅项目原有警告）<br>✅ 分支 `uat-20260729-peter-fca-monthlysum-1403` 已推送 Azure DevOps（commit `ec396fbd2d1`，仅 1 文件 +17/-4，commit 关联 #1403；远程工作区已切回 uat）<br>✅ PR #6345 已合并到 uat（merge commit `fae9430c32f`，用户操作）<br>✅ 已拉取合并后 uat 重编译（0 错误）并更新 DEV1 主 Assembly `SanyD365.D365Extension.Sales`（ID `9d6ff315`，8419 KB，无 PluginType 差异）<br>✅ 2026-07-29 主 Assembly 正式链路回归通过：同月 2 条（600000+400000）+另一月 1 条（200000），计算日志「历史回款能力=14,279.49 x 0.80 = 11,423.59」（=1,200,000÷12÷7.00305，与独立 Assembly 验证值一致），MAX 初始额度=10,225,319.26 与基线一致无回归；测试 proc/3 条 outstanding 已清理、原记录已恢复（新 ID `361d074d`）<br>⏸️ 待用户 n8n 发布 McsPlugin 到 UAT 后 UAT 验证<br>✅ 2026-07-29 用户 n8n 发布 McsPlugin 到 UAT，UAT Assembly `SanyD365.D365Extension.Sales` ModifiedOn=2026-07-29 15:52 UTC（晚于 DEV1 更新的 12:21 UTC，确认 #1403 代码已到达）<br>✅ 2026-07-29 AI 代用户 UAT 验证通过（客户 Kedai Kek）：造 3 条测试回款（2026-06 两条 600000+400000、2026-05 一条 200000），计算日志「历史回款能力=14,227.18 x 0.90 = 12,804.46」，核算 1,200,000÷12=100,000 RMB ÷ UAT CNY 汇率 7.0288 = 14,227.18 USD 精确一致（同月多条已正确求和）；财务能力 11,361,465.84×0.90、初始额度 10,225,319.26 与 #1336 UAT 基线一致无回归；测试 proc/3 条 outstanding 已全部清理（该客户原无 outstanding 记录） | 陈东忍 2026-07-29 创建；取数范围/锚点/汇率/÷12 均不变，结果与旧写法数学等价（无回归）；无新增 D365 组件 |

| 无 | 【融资管理】融资需求阶段误点【提交立项审批】后记录卡在"审批中"，BPP 无实例、无法再次提交 | 融资管理（`mcs_fsm_data`）BPP | 2026-07-30 | 📋 已关闭 | UAT 测试人员在「融资需求」阶段（mcs_fsm_status=1）点击【提交立项审批】：UAT 部署的 mcs_fsm_data.js 为旧版（无状态前置校验），JS 直接把 mcs_bppstatus 写为 2（审批中）并提示已提交；后端 `FsmDataBppIntegrationPlugin` 检测「融资状态(1)与审批类型(1)不匹配」仅 trace 后静默 return，不回滚 bppstatus，导致记录永久卡在"审批中"、再次提交被前端「已有审批在进行中」拦截（UAT 记录 FSM202607290001 / 8b690ba4-6eff-4e1f-ad59-cac806580213，Plugin Trace 实锤） | `Code/Customizations/Plugins/FinancingManagement/Bpp/FsmDataBppIntegrationPlugin.cs`（3 个「不允许提交」兜底分支由静默 return 改为抛 InvalidPluginExecutionException，事务回滚 bppstatus，前端弹具体原因）<br>`Code/Tools/MetadataTool/Program.cs`（新增通用 `update-record` 命令，数据修复用） | ✅ 2026-07-30 UAT 数据修复：清空该记录 mcs_bppstatus（原为空）<br>✅ 修复后真实提交验证通过：bppstartapi 返回成功 → BPP 实例 workflowid=870642051797164032、审批链接/状态码 Submitted 正常回写（UAT Messagehandler 含 Handler["mcs_fsm_data"] 工作正常）<br>✅ 2026-07-30 本地编译 0 错 0 警<br>✅ DEV1 独立 Assembly（临时子类 FsmDataBppIntegrationGuardTestPlugin 避 2601）3 场景实测通过：TC1 状态空+立项审批→抛「当前融资状态不允许提交该类型审批…」且事务回滚（bppstatus/approve_type 仍为空）；TC2 融资立项+can_initiated=0→抛「当前记录不允许提交立项审批（可提交立项标记为否）」且回滚；TC3 融资立项+can_initiated=1→正常提交，bppstartapi 返回成功、bppstatuscode=Submitted<br>✅ 测试记录已复原（FSM202607250002/0003 字段还原）；DEV1 临时 Assembly 已注销（Step/Type/Assembly 全清，红线执行完毕）、主 Assembly Step 已恢复启用、临时子类文件已删<br>✅ 已同步 tx-windows（sync 脚本被 CofaceDataSyncPlugin 历史校验失败阻断，改单文件手动转换：命名空间替换+CRLF，远程 git diff 确认仅本次改动 +8/-6）<br>✅ 远程 Release 编译 0 错误（仅项目原有警告）<br>✅ 分支 `uat-20260730-peter-fsm-bpp-guard` 已推送 Azure DevOps（commit `9c193286f50`，仅 1 文件 +8/-6，基于最新 origin/uat；远程工作区已切回 uat）<br>✅ PR 已合并到 uat（用户操作，2026-07-30；合并后 uat HEAD `21ef72d8ecc`）<br>✅ 已拉取合并后 uat 重编译（0 错误）并更新 DEV1 主 Assembly `SanyD365.D365Extension.Sales`（ID `9d6ff315`，8419 KB）<br>✅ 主 Assembly 正式链路回归通过：空状态记录提交立项审批被拦截报「当前融资状态不允许提交该类型审批…」，事务回滚字段全为空（Trace 实锤为主 Assembly 插件抛出）<br>✅ 2026-07-30 UAT `mcs_fsm_data.js` 三方（UAT/DEV1/本地）MD5 逐字节一致（0b2081a2，44481 bytes），已含状态前置校验（WebResource 已由他人发布带齐），无需单独再发<br>⏸️ 待用户 n8n 发布 McsPlugin 到 UAT 后 UAT 验证 | 测试人员 UAT 反馈，无禅道编号；另发现「当前审批人」字段错位（表单绑 mcs_nextapprover、Handler 写 mcs_bppapprover），由用户自行调整；JS 旧版问题靠发布 McsWebResource 解决 |
| 1433 | 【融资资源管理】列表【激活/停用】按钮点击后"是否启用"列不更新（启用停用功能不生效） | 融资资源管理（`mcs_fsm_resource`） | 2026-07-30 | ✅ 已修复待验证 | 系统【激活/停用】按钮只改 `statecode/statuscode`，`mcs_fsm_resource` 无任何 Plugin/JS 回写自定义字段；且 DEV1 `mcs_fsm_rl_status` 字段标签误建为「是否启用/Is Active」（本地定义 JSON 与 PRD 均为「是否启用过/Ever Enabled」，语义=启用过后置是、此后不可删除）。附带发现：DEV1 Delete 管道 `context.UserId` 恒为 SYSTEM（Trace 实锤），真实操作人必须取 `InitiatingUserId`（Update 管道两者一致） | `Code/Customizations/Plugins/FinancingManagement/Resource/FsmResourceStateSyncPlugin.cs`（新增：Update Filter=statecode PostOp，激活时幂等回写 `mcs_fsm_rl_status=true`，单向标记）<br>`Code/Customizations/Plugins/FinancingManagement/Resource/FsmResourceDeleteGuardPlugin.cs`（新增：Delete PreOp，仅 未启用过+创建人(按 InitiatingUserId 比对) 可删，SysAdmin 放行；业务角色不写死靠安全角色删除权限配置）<br>`Code/Tools/sync-plugin-to-remote.py`（新增 Resource 两文件映射；新增 `--only` 参数绕过 CofaceDataSyncPlugin 历史校验失败；build_remote `cd /d` 改 `cd` 兼容 PowerShell）<br>DEV1 元数据：`mcs_fsm_rl_status` 标签纠错 是否启用→是否启用过（2052）/Is Active→Ever Enabled（1033）并发布实体 | ✅ 2026-07-30 本地编译 0 错 0 警<br>✅ DEV1 临时 Assembly `SanyD365.Plugins.FinancingManagement` 验证 7/7：新建保持 Active 可编辑 rl_status=否 ✅ → 停用→激活 rl_status 自动=是 ✅ → 再停用/再激活幂等保持是 ✅ → 删已启用记录拦截「该融资资源已启用过，不允许删除。」✅ → 创建人删未启用记录成功 ✅（首轮误拦，根因=Delete 管道 UserId=SYSTEM，改 InitiatingUserId 后通过）<br>✅ 测试数据已清理；临时 Assembly 已注销（Step×2/Type×2/Assembly 全清，红线执行完毕）<br>✅ 已同步 tx-windows（--only 两文件，PluginBase 转换+csproj 自动完成），拉平 uat（落后 7 提交）后重编译 0 错误<br>✅ 分支 `uat-20260730-peter-fsm-resource-1433` 已推送（commit `2e87ac1e9d1`，仅 3 文件 +221，commit 关联 #1433；远程工作区已切回 uat）<br>✅ PR 已合并到 uat（用户操作，2026-07-30）；已拉取合并后 uat 重编译 0 错误<br>✅ 已更新 DEV1 主 Assembly `SanyD365.D365Extension.Sales`（ID `9d6ff315`，8423 KB）<br>✅ 主 Assembly 正式 Step 已注册（平台未自动扫出新 Type，用 register-plugin-advanced 显式创建）：StateSync Update/statecode PostOp（Step `3b449718`；首个 `2cfa6d37` 注册后立即加 Solution 报 does not exist，已删除重注册换新 ID）+ DeleteGuard Delete PreOp（Step `f0ff8ab9`）+ PreImage（mcs_fsm_rl_status+createdby），均启用<br>✅ 主 Assembly 正式链路回归通过：停用→激活 rl_status=是、删已启用记录拦截（Trace 实锤 `D365Extension.Sales.Plugins.FinancingManagement.Resource.*` 触发，PluginBase InnerExecute 正常）；测试记录已清理<br>✅ 主清单 AllComponent_Peter_NoUAT 已加 2 个 Step（componenttype=92；教训：PluginType(90) 不能显式 add-solution-component，随 Step 隐式入包）；重建 Step `3b449718` 二次回归通过：停用→激活 rl_status=是、删除拦截实锤；测试样本 FSMR-TEST-1433D（已启用过，按规则不可删）留存<br>✅ DEV1 标签纠错已生效：LCID=1033 Ever Enabled / LCID=2052 是否启用过，实体已发布<br>⏸️ 待用户 DEV1 界面验证（列表点【停用】→【激活】后「是否启用过」列变「是」、删除按钮拦截提示）<br>⏸️ 待用户 n8n 发布 UAT（McsPlugin + entity 包带标签） | 方案经用户确认（2026-07-30）：①标签纠错「是否启用过」；②激活回写 Plugin；③删除守卫（角色不写死靠删除权限配置）；④PRD「新增默认停用」不落 statecode（避免保存后只读），由「是否启用过=否」承担草稿态语义，「启用」=停用→激活；删除按物理删除落地。<br>⚠️ **新增 D365 组件**：2 个 Step 已加主清单 AllComponent_Peter_NoUAT（2026-07-30，componenttype=92；PluginType 随 Step 隐式入包无需单独加）；发版包 McsPlugin 的 Assembly+Step 待用户分布；<br>⚠️ **环境配套（配置数据，用户手动）**：「融资资源管理员」安全角色需配置 `mcs_fsm_resource` Delete 权限，其他角色不给（DEV1/UAT/生产）；已登记《D365配置数据清单》；<br>⚠️ 存量数据处理：已有记录 rl_status 全为「否」，首次 停用→激活 后即变「是」不可删，属预期语义
| 无 | 【融资管理/厂端授信】当前审批人字段错配：代码回写 mcs_bppapprover，表单与 BPP 框架约定均为 mcs_nextapprover | 融资管理（`mcs_fsm_data`）+ 厂端授信（`mcs_fca_quotaapp`）BPP | 2026-07-30 | ✅ 已修复待验证 | BPP 框架 `BPPService.cs` 通用逻辑写死绑定 `mcs_nextapprover`（发起时回写 mcs_workflowid+mcs_nextapprover、结束时清空，除 mcs_quoterdetail 外全实体生效），且 FsmData/FCA 表单「当前审批人」均绑 mcs_nextapprover；但实体专属 Handler（FsmData/FcaQuotaApp）发起与回调时回写的是 `mcs_bppapprover`，D365 回调 Plugin 撤回/废弃时清空的也是 `mcs_bppapprover`——当前审批人永远不显示。另：UAT 实测框架与 Handler 调 GetNextApprover 均返回空（mcs_nextapprover/mcs_bppapprover 双空），审批人未解析属 BPP 模板侧问题，与本修复分开跟踪 | `Code/SanyD365Project/Service/SanyD365.Main/Entities/BPP/BPPHandlerServices/BPPHandlerServiceForFsmData.cs`（2 处回写改 mcs_nextapprover）<br>`Code/SanyD365Project/Service/SanyD365.Main/Entities/BPP/BPPHandlerServices/BPPHandlerServiceForFcaQuotaApp.cs`（2 处回写改 mcs_nextapprover，过时注释修正）<br>`Code/Customizations/Plugins/FinancingManagement/Bpp/FsmDataBppCallbackPlugin.cs`（撤回/废弃清空改 mcs_nextapprover）<br>`Code/Customizations/Plugins/FactoryCredit/Bpp/FcaQuotaAppBppCallbackPlugin.cs`（撤回/废弃清空改 mcs_nextapprover） | ✅ 2026-07-30 本地 4 文件修改完成，Plugin 两项目本地编译 0 错<br>✅ 已同步 tx-windows（4 文件 git diff 核实 +13/-13，纯字段名+注释）<br>✅ `SanyD365.D365Extension.Sales` 远程 Release 编译 0 错误<br>✅ DEV1 独立 Assembly（临时子类×2 避 2601）验证通过：FSM/FCA 写入测试审批人后模拟 Withdrawn 回调，`mcs_nextapprover` 均正确清空、bppstatus 回 1；临时 Assembly×2 已注销、主 Step×2 已恢复、测试数据已复原、临时类已删（红线执行完毕）<br>✅ uat 编译已恢复（苜坚 PR 6414 修复 CS0023），`SanyD365.Main` Release 编译 0 错误<br>✅ 分支 `uat-20260730-peter-bpp-nextapprover-fix` 已推送 Azure DevOps（commit `dcdb08a5feb`，4 文件 +13/-13，基于最新 origin/uat；远程工作区已切回 uat）<br>✅ PR 6415 已合并到 uat（merge `31c3fe298b8`，用户操作）<br>✅ 已拉取合并后 uat 重编译（0 错误）并更新 DEV1 主 Assembly `SanyD365.D365Extension.Sales`（ID `9d6ff315`，8429 KB）<br>✅ 主 Assembly 正式链路回归通过（FSM+FCA）：写入「回归测试审批人」→ 模拟 Withdrawn 回调 → `mcs_nextapprover` 正确清空、bppstatus 回 1；FSM 测试记录已复原<br>⏸️ 待用户 n8n 发布 McsPlugin + Messagehandler 到 UAT → UAT 验证（前提：BPP 团队先解决融资立项模板审批人解析为空问题） | 用户代码评审发现，无禅道编号；部署：Service 2 文件走 n8n Messagehandler 发布，Plugin 2 文件走 McsPlugin 发布 |
| 无 | 【融资管理】提交融资方案审批提示「以下必填字段未填写：mcs_fsm_credit_amount_usd」，界面该填的都填了仍被阻断 | 融资管理（`mcs_fsm_data`） | 2026-07-31 | ✅ 已修复待验证 | 三重缺口叠加：①`mcs_fsm_credit_amount_usd`（融资金额USD）未放在主表单上（DEV1 FormXml 实锤），用户无处可填，弹窗显示逻辑名而非标签正是控件缺失的表现；②PRD 设计的「根据授信金额自动转换为USD存入」折算逻辑从未实现（JS/Plugin 均无）；③JS 提交校验 `REQUIRED_SOLUTION_EXTRA` 把它列为必填且 `validateRequiredFields` 只读表单属性（字段不在表单上恒 null），永远判空阻断 | `Code/Customizations/WebResources/JS/mcs_fsm_data.js`（①CACHE_FIELDS 加入 mcs_fsm_credit_amount_usd，validateRequiredFields 改用 getFieldValue 缓存回退读服务端值；②新增 updateCreditAmountUsd：授信金额/融资币种 onChange 按 D365 交易货币汇率自动折算 USD（组织基础币种=USD，公式=金额÷汇率），Xrm.WebApi 直写服务端并刷新缓存，值未变化不写、币种/金额为空不算；③onLoad 对存量 usd 空记录自动补算） | ✅ 2026-07-31 UAT 数据修复：`FSM202607300009`（28b766de-4ab9-4af6-872d-176aa3348af9）授信金额 234,234,234.00 BHD ÷ 汇率 0.3769（UAT 交易货币表，基础币种 USD）= 621,475,813.21 已回写 `mcs_fsm_credit_amount_usd`（update-record，回读确认）<br>✅ node 语法检查通过；Node 仿真验证 6/6：服务端有值校验通过/空值仍拦截（兜底）/自动折算 621475813.21 与手工修复值一致/值未变不重复写/金额变更重算正确/币种空不写不报错<br>✅ 2026-07-31 已部署 DEV1 并发布（47760 bytes，部署前 diff 确认 DEV1 与本地修改前逐字节一致，部署后回读与本地逐字节一致）<br>✅ 2026-07-31 用户确认以仿真验证为准，跳过 DEV1 界面验证<br>✅ 2026-07-31 用户 n8n 发布 McsWebResource 到 UAT 完成（前两次误发 McsCustomAPI，排查导入历史后纠正）；UAT 回读 47760 bytes，MD5 `95f47c6b` 与 DEV1/本地三方逐字节一致，测试人员可继续（提醒强刷浏览器）<br>⚠️ UAT `FSM202607300012`（阶段3，授信金额 1,000,000 RON，usd 空）同受此 Bug 影响，新 JS 发布后打开表单即自动补算，无需手工修数据 | 测试人员 UAT 直接反馈（截图 FSM202607300009），无禅道编号；修复方案经用户确认=自动折算（PRD 原文「根据授信金额自动转换为USD存入」），字段不上表单只存库；无新增 D365 组件，无需加主清单 |
| 无 | 【融资管理】按钮显隐优化：新增时【提交立项审批】【提交融资方案审批】不可见；立项审批通过后【提交立项审批】不可见 | 融资管理（`mcs_fsm_data`）命令栏按钮 | 2026-07-31 | ✅ 已修复待验证（2026-08-01 DEV1 三阶段界面验证全部通过） | 两个提交按钮为现代 appaction（`mcs_fsm_data_submit_initiation/project`），无任何显隐控制常显；显隐口径经用户确认按 PRD：立项按钮仅状态=2（融资立项）可见、方案按钮仅状态=3（融资解决方案）可见、新增未保存均隐藏（角色判断 Ribbon 同步规则无法做，保持 JS 现有拦截）；技术路线经用户确认=经典 Ribbon 内联显隐规则（随实体包零手动，参考成交条件样板库架构），按钮尽量往前 | `Code/Customizations/Ribbon/mcs_fsm_data.ribbon.xml`（新增：2 个表单 Ribbon 按钮 Sequence=10/11+ModernImage=CheckMark+双语 LocLabels，DisplayRule CustomRule 调 JS 同步函数）<br>`Code/Customizations/WebResources/JS/mcs_fsm_data.js`（新增 ribbonShowSubmitInitiation/ribbonShowSubmitProject 同步显隐函数：未保存隐藏+按 mcs_fsm_status 阶段判断）<br>`Code/Tools/MetadataTool/Services/EntityManager.cs`（新增 DeployRibbonDiff 全链路：导出载体Solution→合并实体 RibbonDiffXml→重打包→导入→发布；新增 ImportSolution）<br>`Code/Tools/MetadataTool/Program.cs`（新增 deploy-ribbon/import-solution 命令） | ✅ 2026-07-31 JS 已部署 DEV1（49280 bytes，回读与本地 MD5 一致）<br>✅ 2026-07-31 Ribbon 部署：导出 entity_20260713→幂等合并（CustomActions/CommandDefinitions/LocLabels/DisplayRules 各 2-4 节点）→重打包 98KB→导入成功（⚠️ 客户端 30 分钟超时被杀但服务端导入实际已完成，导入历史实锤 2 次「导入-更新-成功」；发布步骤未执行被用户发现）→补 publish mcs_fsm_data 完成<br>✅ 2026-07-31 重新导出回读验证：12 个 mcs.mcs_fsm_data.* Ribbon 节点全部在场<br>✅ 2026-07-31 DEV1 旧 appaction×2 已删除（list-app-actions 回读 0 条），主清单 AllComponent_Peter_NoUAT 自动同步移除（非托管删除联动，无需手动）<br>✅ 2026-08-01 DEV1 界面验证全部通过（Playwright 实测+用户截图互证）：阶段2 FSM202607250005 仅显示【提交立项审批】、阶段3 FT0717062823 仅显示【提交融资方案审批】、状态空 FSM202607250003 两按钮均隐藏；按钮直接落在主命令栏「流程」与「分派」之间（未进溢出菜单）<br>✅ 2026-08-01 不可见根因定论：UCI 服务端客户端元数据 blob 过期（导入+发布后未自动重建，UserMetadataVersion 未变），命令检查器「Regenerate ribbon metadata」重建后正常；官方文档已核对 XML 写法（ValueRule/Location/LocLabels 结构）无误<br>✅ 2026-08-01 UAT 发布完成并验证通过：JS（49280/0a976a39 与 DEV1/本地三方一致、无非托管层遮挡）先于实体包到 UAT；实体包发布后生效 Ribbon 2 按钮+2 CustomRule 在场；用户已在 UAT Command Designer 隐藏 2 个旧 appaction；Playwright 全新浏览器实测 FSM202607300009（阶段3）主命令栏仅显示【提交融资方案审批】（data-id=mcs.mcs_fsm_data.FormSubmitProject.Button 实锤经典 Ribbon 按钮非旧 appaction）；⚠️ 用户浏览器看不到=本地命令栏元数据缓存，强刷即可 | 用户直接提出优化需求，无禅道编号；UCI 平台限制经典 Ribbon 按钮进「更多命令」溢出菜单（Sequence 调优尽量靠前，参照成交条件 r3 方案）；UAT 旧 appaction 残留需发布后手动隐藏（非托管导入叠加不删除，历史教训）；⚠️ 教训：ImportSolutionRequest 服务端异步执行，客户端被杀不代表失败，重复导入前先查导入历史；元数据实体 entity 不支持 ribbondiffxml 直接读写，实体 Ribbon 必须走 Solution 导入；无新增需加主清单组件（Ribbon 随实体，JS 已在 McsWebResource）<br>⚠️ 教训（2026-08-01）：UCI 表单经典 Ribbon 排障三板斧——①生效 Ribbon 用 RetrieveEntityRibbonRequest 验证结构；②命令检查器（&ribbondebug=true）看规则求值；③界面不显示优先考虑客户端元数据 blob 过期，用命令检查器「Regenerate ribbon metadata」重建（耗时 15min+，期间会阻塞 Solution 导出等后台任务）；Command Designer 只显示现代按钮，经典 Ribbon 按钮不在其中属预期 |
| 无 | 【融资管理】方案审批通过后点「下一阶段」误报「请先提交立项审批并通过后，才能进入融资解决方案阶段」，且刷新表单 BPF 阶段条不跟随状态字段 | 融资管理（`mcs_fsm_data`）BPF 阶段门禁/同步 | 2026-08-01 | 🔧 已修复待 UAT 终验 | 双层根因：①门禁 `preventBpfNextWithoutApproval` 用单字段 `mcs_approve_type`（只存最近一次审批类型）推断"立项已通过"，方案审批后 type 被覆盖为 2，`initiationPassed=(type===1&&bpp===3)` 恒 false → 进融资落实被第一道门以错误提示拦截（UAT 实锤：FSM202607310002，type=2/bpp=3/status=4）；②`syncBpfFromStatus` 用 `setActiveStage` 前进到未走过阶段，官方文档明确该 API 仅用于"设置已完成（已走过）阶段为活动阶段"，前进一律返回 `invalid`（UAT/DEV1 各实测一次实锤），导致 reconcileStagesOnLoad 永远无法让阶段条追上状态字段；正确前进 API 为 `moveNext()`（UI「下一阶段」同款，会触发 PreStageChange） | `Code/Customizations/WebResources/JS/mcs_fsm_data.js`（①initiationPassed 增加 `‖ approveType===PROJECT`——方案审批存在⟹立项必然已通过；②syncBpfFromStatus 改 moveNext 循环前进+门禁加 `_reconcilingFromStatus` 程序化豁免标记，避免 onLoad 阶段 _fieldCache 未就绪误拦） | ✅ Node 仿真 18/18（门禁矩阵 12 场景 + moveNext 循环 6 场景）<br>✅ 2026-08-01 已部署 DEV1 并发布（50851 bytes，回读 MD5 与本地一致）<br>✅ DEV1 Playwright 实测：①reconcile 自动跟进通过——a77c3b07 手工造脱节（status=3/BPF=2）刷新表单后 BPF 自动 moveNext 到融资解决方案；②门禁拦截正确——FSM202607250005（立项审批中 type=1/bpp=2）点「下一阶段」被拦且提示语正确（截图实锤），阶段未动；③方案已通过记录（FT0717062823 type=2/bpp=3）进融资落实不再被第一道门误拦（旧版必误报立项提示）；④moveNext 返回 invalid（必填未齐/实例异常）时安全中止不循环<br>✅ 实测中抓出并修复新增边界 Bug：moveNext 回调触发时 getActiveStage() 竞态仍返回旧阶段，旧循环按移动后重读会多发一次 moveNext，目标为最终阶段时把流程直接推成 completed（FT 实证，流程条消失）；已改为预算式推进（movesLeft=status-bpfNum，最多 N 次绝不多发），Node 仿真同步通过<br>✅ 测试数据已清理：a77c3b07 复原（status=2/BPF=融资立项，backing 实例直接改回）；FT0717062823 原 BPF 实例在 overshoot 实证中被推 completed 消失，已手工重建实例（阶段=融资解决方案）并补齐方案必填字段，⚠️ 手工实例 moveNext 恒 invalid（非原生实例平台拒绝移动），成功落入最终阶段场景只能在真实完整数据终验<br>⏸️ 待走 UAT 发布后在 FSM202607310002 终验：刷新表单 reconcile 应自动 moveNext 到融资落实（该记录必填齐全，moveNext 应成功且预算=1 保证不越界） | 用户 UAT 测试反馈（FSM202607310002），无禅道编号；修复后用户记录无需手工数据修复——刷新表单 reconcile 经 moveNext 自动推进阶段条；⚠️ 教训：①BPF 程序化前进必须用 moveNext（setActiveStage 仅用于回退到已走过阶段，且要求选中阶段=活动阶段）；②moveNext 回调里 getActiveStage() 有竞态，循环次数必须预算式上限控制，最终阶段多移一次会把流程推成 completed 且不可撤销 |
| 无 | 【融资管理】附件 tab「加载数据失败！」，文件类型下拉空白 | 融资管理（`mcs_fsm_data`）附件 | 2026-08-01 | ✅ 已修复待验证 | 双重缺配：①平台配置 `UploadFileTypeMapping`（`ms_systemconfiguration`）从未配置 `mcs_fsm_data` 的附件类型映射（DEV1/UAT 同缺，该 JSON 为全系统 130+ 实体共用），通用上传组件调 Custom API `mcs_GetUploadFilePageInitInfo` 后端抛 400「找不到实体名称为mcs_fsm_data的上传文件类型映射」（Plugin Trace 实锤 `SanyD365.D365ExtensionApi.Apis.GetUploadFilePageInitInfo`）；②语言包缺 `mcs_fsm_data_filetype_*` key 导致下拉标签空白。PRD 依据：融资管理附件表 `mcs_fsm_files.mcs_filetype`=001融资资源/002融资需求/003需求管理/004融资方案/099其他 | 配置数据（非代码）：`UploadFileTypeMapping` 纯追加 `"mcs_fsm_data": [{001,002,003,004,099}, Max=10]`（Max 参照 mcs_credit_record/fca_quotaapp 既有配置）<br>语言包：`Language/2052.json`+`1033.json` 各纯追加 5 key（`mcs_fsm_data_filetype_001~004,099`）；远程仓库 `D365/SanyD365.D365WebResource/WebResource/ms_languagefile_2052.json`（5973→5978）/ `ms_languagefile_1033.json`（5984→5989）纯追加 | ✅ 2026-08-01 UAT 配置追加完成（171→172 key，JSON 校验既有 key 零丢失），整页刷新后附件 tab 实测恢复正常（列表头+无数据，不再报错；首轮仍 400 为 Plugin 服务端配置缓存，等缓存过期/新 worker 后自愈）<br>✅ 2026-08-01 DEV1 配置追加完成（165→166 key）<br>✅ 2026-08-01 DEV1 语言包曾直连部署验证下拉标签正常（融资资源/融资需求/需求管理/融资方案），后按用户规则（语言包唯一通道=仓库+git 发布，禁止直连环境）用备份回滚并 MD5 逐字节验证复原<br>✅ 分支 `uat-20260801-peter-langfile-fsm-datafiletype` 已推送（commit `d291400f48e`，2 文件纯追加各 5 行，基于最新 origin/uat；远程工作区已切回 uat）<br>✅ PR 6568 已合并到 uat（merge `052e8fc7986`，用户操作）；远程工作区已 fast-forward 拉平<br>✅ 2026-08-01 DEV1 语言包以仓库 uat 版正式发布（2052: 5978 keys / 1033: 5989 keys，回读 MD5 与仓库逐字节一致），Playwright 界面验证附件下拉标签正常显示（融资资源/融资需求/需求管理/融资方案）<br>✅ 清单已登记：《上线核对清单》2.4.8 +《D365配置数据清单》均补 `mcs_fsm_data` 附件映射条目<br>⏸️ 待用户 n8n 发布 McsWebResource 到 UAT → UAT 语言包生效（发布后三方一致性核对 + 界面验证，提醒测试人员强刷浏览器） | 用户 UAT 界面发现（FSM202607310002 附件 tab 截图），无禅道编号；配置改动严格「只追加不覆盖」红线；⚠️ 教训：①新实体接通用上传组件必须同步配 `UploadFileTypeMapping` + 语言 filetype key，并随开发即登记清单；②语言包任何环境都不直连改，一律仓库 git 发布生效 |
