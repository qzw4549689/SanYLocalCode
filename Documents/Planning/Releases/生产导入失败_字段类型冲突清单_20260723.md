# 生产导入失败 — 字段类型冲突清单（2026-07-23）

> **背景**：上次生产大版本 = 2026-07-03 23:00；本次发版（`entity_20260722` 批次）导入生产失败，报错 `80041A06`：
> `Attribute mcs_applygenre is a Picklist, but a String type was specified.`
> 导入在第一个冲突字段处即中断，后面可能还有其他冲突字段，本文给出完整排查结果。
>
> **排查方法**：
> 1. UAT `importjob` 历史全量扫描（MetadataTool 新命令 `list-failed-imports`，127 条失败记录；⚠️ UAT 导入历史仅保留到 2026-06-21，更早的已被平台清理）
> 2. 本地三份历史快照与 DEV1 当前字段清单逐字段 diff（`Backups/Tests/field_type_audit_20260723/diff_field_types*.py`）：
>    - 6/3 `entity_20260603_peter_backup` XML（3 实体）
>    - 6/8 `Code/SanyD365Project/solutions/unpacked_latest` pac 解包（8 实体）
>    - 6/21 `entity_20260603_peter` XML（15 实体）
> 3. 用户提供的生产/发版包截图实锤

---

## 一、必须从生产删除的字段（3 个，已实锤）

| # | 实体（逻辑名 / 显示名） | 字段架构名 | 显示名 | 生产当前类型 | 本次发版包类型 | 证据 |
|---|------|-----------|--------|-------------|---------------|------|
| 1 | `mcs_fca_quotaapp`<br>厂端授信额度申请单表（Factory Credit Quota Application） | `mcs_applygenre` | 信保额度类型 / Credit Insurance Quota Type | **Choice（选项）** | **Single line of text（单行文本）** | ✅ 生产导入报错原文点名<br>✅ 截图实锤（生产=Choice，entity_20260722=Single line）<br>✅ UAT 7/8 `entity_20260708_peter` 导入同样失败于该字段<br>变更记录：07-07 DEV1 先删后建（Picklist→String） |
| 2 | `mcs_trade_stpayterm`<br>成交条件样板库（Trade Payment Terms Baselines） | `mcs_buyergrade` | 客户分类代码 / Customer Category Code | **Single line of text（单行文本）** | **Choice（多选选项集 MultiSelectPicklist）** | ✅ 截图实锤（生产=Single line，entity_20260722=Choice）<br>✅ UAT 6/25 `entity_20260603_peter` 导入失败：`OptionSetId cannot be null for existing attribute mcs_buyergrade`<br>变更记录：06-21~23 String→MultiSelectPicklist |
| 3 | `mcs_trade_stpayterm`<br>成交条件样板库（Trade Payment Terms Baselines） | `mcs_creditgrade` | 客户等级 / Customer Grade | **Single line of text（单行文本）** | **Choice（选项 Picklist）** | ✅ UAT 6/22 `entity_20260603_peter` 导入两次失败：`OptionSetId cannot be null for existing attribute mcs_creditgrade`<br>✅ 与 `mcs_buyergrade` 同批次变更（06-21~23），生产的 buyergrade 已实锤为旧 String，creditgrade 必然同为旧 String<br>变更记录：06-21~23 String→Picklist |

**变更根因**：这三个字段都是「先删除旧字段、再以同名新建不同类型字段」，导致生产已存在的同名字段与发版包中字段类型不一致，托管 Solution 导入校验直接失败（`80041A06` / `80041A08` 系列）。D365 不允许通过导入改变已存在字段的类型。

## 一之二、6/1~6/21 期间的变更（UAT 日志已覆盖不到，靠本地快照 diff 发现）

| # | 实体（逻辑名 / 显示名） | 字段架构名 | 显示名 | 旧类型 | 新类型 | 变更时间窗 | 风险评估 |
|---|------|-----------|--------|--------|--------|-----------|---------|
| 4 | `mcs_credit_scoringcard`<br>客户评分卡配置表 | `mcs_listvalue` | 定性项目值 / Qualitative Item Value | **Single line of text（6/3 快照=nvarchar）** | **Lookup（→ mcs_credititem_value）** | 6/3 ~ 6/8 | ⚠️ **需三先生成核对**：若生产上的评分卡实体是 6/8 之前导入的旧版（listvalue 为单行文本），则同样冲突，需删除；若生产是 7/3 才首次导入该实体，则无冲突 |
| 5 | `account`（客户，共享实体） | `mcs_creditgrade` | 客户等级 | **Single line of text（6/8 快照=nvarchar）** | **Choice（Picklist）** | 6/8 ~ 6/15 | ⚠️ 低风险：account 为共享实体，**不在本次 entity_20260722 包内**，仅当其他发版包携带 account 组件时才冲突；建议顺手请三一生成核对，若是 String 则处理 |

## 二、不阻塞导入，但建议一并清理（可选）

| 实体（逻辑名 / 显示名） | 字段 | 说明 |
|------|------|------|
| `mcs_trade_stpayterm`<br>成交条件样板库 | `mcs_nation`（Lookup→国家） | 06-25 多选改造后已废弃，DEV1 已删除；生产若存在，update 模式导入**不会报错也不会删除**，建议手动清理避免混淆 |
| `mcs_trade_stpayterm`<br>成交条件样板库 | `mcs_trade_pttype`（Lookup→产品分类） | 同上，废弃 Lookup，功能由 `mcs_trade_type`（Memo）替代 |

## 三、已排除项（核实无冲突，不用动）

| 字段 | 核实结论 |
|------|---------|
| `mcs_fca_proc.mcs_creditreject` | 07-01 在 DEV1 删除重建（同名同型，仅 MetadataId 变化）。7/3 生产大版本已成功导入包含重建后字段的包（7/3 发布成功 → 说明当时无冲突），生产当前已是新版本，本次无需处理 |
| `mcs_fsm_*`（融资管理 3 实体全部字段） | 07-13 才在 DEV1 创建，生产不存在这些实体，属于全新组件，不会冲突 |
| `mcs_credit_scoringcard.mcs_weight` | 仅为取值范围 `[0,99]→[-100,100]` 调整，范围变更可正常导入，非类型变更 |
| `mcs_fca_quota.mcs_sellerbalance` / `mcs_fca_records.mcs_asisbalance/tobebalance` | 同上，仅为最小值放宽，非类型变更 |
| `mcs_fca_quota.mcs_usedsellerbalance` | 本次全新字段，生产不存在，不会冲突 |

## 四、其他团队的同类问题（提醒，不属于我们的包）

| 字段 | 冲突 | 来源 |
|------|------|------|
| `mcs_shipping_factory` | 环境中=String，包中=Lookup | UAT 7/18 `CPQ_entity_20260713_temp` 导入失败。若本次生产发版包含 CPQ 相关包，对应团队需同样处理 |

## 五、给三一操作建议

1. **删字段前**：先将需删除的字段从生产环境所有表单、视图中移除（否则删除被阻止）。
2. **数据备份**：`mcs_applygenre`（生产为 Choice）和 `mcs_buyergrade`/`mcs_creditgrade`（生产为 String）中若已有业务数据，删除即丢失，请先导出备份。
3. **执行删除**：在生产环境（Power Apps → 表 → 列）删除第一节表中的 3 个字段。
4. **核对两个疑似字段**：检查生产 `mcs_credit_scoringcard.mcs_listvalue` 是否为 Single line of text（是→同样删除）；检查 `account.mcs_creditgrade` 是否为 Single line of text（是→若发版包带 account 组件需处理）。
5. **重新导入**：删除完成后重新导入 `entity_20260722` 批次 Solution。若再次报错，报错信息会直接点名下一个冲突字段，按同样方式处理。
6. **可选清理**：顺手删除第二节的 2 个废弃 Lookup 字段。

---

*附件：排查过程原始数据存于 `Backups/Tests/field_type_audit_20260723/`（DEV1 二十实体字段清单、UAT importjob 失败记录全文）。*
