# 上线数据处理专用目录

> 创建于 2026-08-25。用于集中处理生产上线时的数据导入工作：用户提供原始数据 → 解析核对 → 生成生产导入产出。
> 背景与方案详见 `Memory.md` 2.31 节。

## 目录结构

```
上线数据/
├── README.md            # 本文件
├── 01_用户原始数据/      # 用户提供的原始 Excel（评分卡、客户↔CofaceID 对应表等），只读不改
├── 02_解析核对/          # 解析脚本输出的核对报告（区间连续性、赋分加总、与 DEV1 差异对比等）
└── 03_生产导入产出/      # 最终生成给生产导入用的文件（Excel / dry-run 清单 / 跑前快照等）
```

## 当前数据清单

| 数据 | 来源 | 状态 |
|------|------|------|
| 评分卡（2 Sheet：经销商评分卡(新老经销商)、直销评分卡(新老SA)） | 飞书 `shtk5uH4E5H8krWVIBb0EEc1bed`（2026-08-25 用户复制到本地 `01_用户原始数据/评分卡-0825.xlsx`） | ✅ 已核对，报告见 `02_解析核对/评分卡0825_核对报告.md`，3 个问题待用户拍板 |
| 客户ID↔CofaceID 对应关系 | 用户待提供 | ⏳ 未提供 |

## 工作原则

- `01_用户原始数据/` 中的文件**只读**，解析脚本不得修改原件。
- 解析/处理脚本统一放 `.venv_excel/`（沿用现有 Excel 处理脚本的存放习惯），本目录只放数据和报告。
- 临时截图、中间调试产物仍放 `Backups/TempTest/`，不放这里。
